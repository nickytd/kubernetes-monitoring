apiVersion: v1
kind: ConfigMap
metadata:
  name: blackbox-targets
  namespace: monitoring
data:
  blackbox-targets.json: |
    [
      { 
        "targets":["https://grafana.local.dev"],
        "labels" :{
            "application": "grafana"
         }
      },
      { 
        "targets":["https://prometheus.local.dev"],
        "labels" :{
            "application": "prometheus"
         }
      },
      { 
        "targets":["https://alertmanager.local.dev"],
        "labels" :{
            "application": "alertmanager"
         }
      },
      { 
        "targets":["https://blackbox-exporter.local.dev"],
        "labels" :{
            "application": "blackbox-exporter"
         }
      },
      { 
        "targets":["https://karma.local.dev"],
        "labels" :{
            "application": "karma"
         }
      }
    ]  