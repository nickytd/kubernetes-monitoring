apiVersion: v1
kind: ConfigMap
metadata:
  name: blackbox-targets
  namespace: monitoring
data:
  blackbox-targets.json: |
    [
      { 
        "targets":["http://grafana.local.dev"],
        "labels" :{
            "application": "grafana"
         }
      },
      { 
        "targets":["http://prometheus.local.dev"],
        "labels" :{
            "application": "prometheus"
         }
      },
      { 
        "targets":["http://alertmanager.local.dev"],
        "labels" :{
            "application": "alertmanager"
         }
      },
      { 
        "targets":["http://blackbox-exporter.local.dev"],
        "labels" :{
            "application": "blackbox-exporter"
         }
      },
      { 
        "targets":["http://karma.local.dev"],
        "labels" :{
            "application": "karma"
         }
      }
    ]  