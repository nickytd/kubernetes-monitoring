apiVersion: v1
kind: ConfigMap
metadata:
  name: blackbox-targets
  namespace: monitoring
data:
  blackbox-targets.json: |
    [
      { 
        "targets":["http://grafana.dev"],
        "labels" :{
            "application": "grafana"
         }
      },
      { 
        "targets":["http://prometheus.dev"],
        "labels" :{
            "application": "prometheus"
         }
      },
      { 
        "targets":["http://alertmanager.dev"],
        "labels" :{
            "application": "alertmanager"
         }
      },
      { 
        "targets":["http://blackbox-exporter.dev"],
        "labels" :{
            "application": "blackbox-exporter"
         }
      },
      { 
        "targets":["http://karma.dev"],
        "labels" :{
            "application": "karma"
         }
      }
    ]  